{
  "name": "git-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter './packages/*' dev",
    "build": "pnpm --filter server build && pnpm --filter frontend build",
    "lint": "pnpm --parallel --filter './packages/*' lint",
    "clean": "pnpm --parallel --filter './packages/*' clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
